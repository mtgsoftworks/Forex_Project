/**
 * common modülü:
 * - Ortak veri modelleri (model paketi): Rate, RateFields, RateStatus, RawRateEntity, CalculatedRateEntity
 * - DTO ve Mapper (dto paketi): RateDto, RateMapper (MapStruct)
 * - JPA Repository (repository paketi): RawRateRepository, CalculatedRateRepository
 *
 * Bu modül, projedeki tüm bileşenler tarafından paylaşılan veri yapılarını ve dönüşüm mekanizmalarını içerir.
 */
package com.example.forexproject;
